---
import { ArrowUpRight } from "lucide-react";
import Base from "@/layouts/Base.astro";
import EnlaceBoton from "@/components/ui/EnlaceBoton.astro";
import { crearMetadata } from "@/lib/seo";
import { navPrincipal } from "@/content/site";
import { servicios } from "@/content/servicios";

const meta = crearMetadata({
  title: "Página no encontrada | ZSolutions",
  description: "La página que buscas no existe o ha cambiado de dirección.",
  path: "/404",
  noIndex: true,
});
---

<Base meta={meta}>
  <section class="relative flex min-h-[80svh] flex-col justify-center overflow-hidden py-28">
    <div class="plano absolute inset-0 -z-10 opacity-50" aria-hidden="true"></div>

    <div class="container-brand">
      <p class="text-eyebrow text-brand-blue-soft">Error 404</p>
      <h1 class="mt-5 max-w-2xl text-5xl">Aquí no hay nada instalado</h1>
      <p class="mt-5 max-w-lg text-lg text-fg-muted">
        La página que buscas no existe o ha cambiado de dirección. Estos son los caminos
        más cortos para llegar a lo que necesitas.
      </p>

      <div class="mt-8 flex flex-col gap-3 sm:flex-row">
        <EnlaceBoton href="/" tamano="lg">Volver al inicio</EnlaceBoton>
        <EnlaceBoton href="/contacto" variante="secundario" tamano="lg">
          Solicitar presupuesto
        </EnlaceBoton>
      </div>

      <div class="hairline-t mt-14 grid gap-8 pt-10 sm:grid-cols-2">
        <nav aria-label="Servicios">
          <h2 class="text-eyebrow mb-3 text-fg-dim">Servicios</h2>
          <ul class="flex flex-col gap-2 text-sm">
            {
              servicios.map((servicio) => (
                <li>
                  <a
                    href={`/servicios/${servicio.slug}`}
                    class="inline-flex items-center gap-1.5 text-fg-muted transition-colors hover:text-fg"
                  >
                    {servicio.nombre}
                    <ArrowUpRight className="h-3 w-3" aria-hidden="true" />
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>

        <nav aria-label="Secciones de la web">
          <h2 class="text-eyebrow mb-3 text-fg-dim">La web</h2>
          <ul class="flex flex-col gap-2 text-sm">
            {
              [...navPrincipal, { label: "Contacto", href: "/contacto" }].map((enlace) => (
                <li>
                  <a
                    href={enlace.href}
                    class="inline-flex items-center gap-1.5 text-fg-muted transition-colors hover:text-fg"
                  >
                    {enlace.label}
                    <ArrowUpRight className="h-3 w-3" aria-hidden="true" />
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>
      </div>
    </div>
  </section>
</Base>
